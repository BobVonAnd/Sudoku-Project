package com.sudoku.model;
//This extends node and it is the column headers of the linked list. The int size represents the amount of nodes in it's column
class ColumnNode extends Node {
    int size = 0;
    String label;
}